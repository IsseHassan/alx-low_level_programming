#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 * @int:  absolute value of an integer.
 * Return:  Always 0
 */
int _abs(int)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
